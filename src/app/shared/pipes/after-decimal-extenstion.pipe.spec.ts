import { AfterDecimalExtenstionPipe } from './after-decimal-extenstion.pipe';

describe('AfterDecimalExtenstionPipe', () => {
  it('create an instance', () => {
    const pipe = new AfterDecimalExtenstionPipe();
    expect(pipe).toBeTruthy();
  });
});
