import { NumberToWordsTwelveDigitPipe } from './number.to.words.twelve.digit.pipe';

describe('Number.To.Words.Twelve.DigitPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberToWordsTwelveDigitPipe();
    expect(pipe).toBeTruthy();
  });
});
