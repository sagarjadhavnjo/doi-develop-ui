import { ValidatePaybandValueDirective } from './validate-payband-value.directive';

describe('ValidatePaybandValueDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidatePaybandValueDirective();
    expect(directive).toBeTruthy();
  });
});
