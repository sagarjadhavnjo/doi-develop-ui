import { PayScaleValidationDirective } from './pay-scale-validation.directive';

describe('PayScaleValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new PayScaleValidationDirective();
    expect(directive).toBeTruthy();
  });
});
